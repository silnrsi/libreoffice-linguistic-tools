#
# Files that we read or write one line at a time, as opposed to XML files where
# we read them in a DOM view.
#
# Typical plain text files can normally be written and read with Writer,
# so there does not seem to be a need to have a special reader for such files
# in this package.
#

